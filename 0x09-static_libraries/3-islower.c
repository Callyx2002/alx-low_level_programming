#include "main.h"
/**
 * _islower - checks if the character is lower case or not
 * @c: is a character
 * Return: prints 1 or 0 depending on the condition
 *
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
