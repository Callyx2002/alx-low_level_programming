#include "main.h"
#include <stdio.h>
/**
 * add - checks the code
 * @a - an integer
 * @b - an integer
 * Return: returns a value based on the condition
 */
int add(int a, int b)
{
	int c = a + b;

	return (c);
}
